# compile the two codes, run run1 first to generate the data, then use run2 to calculate
# file A.dat B.dat will be generated by run1, and be read in by run2. run2 will produce
# numsoln.dat for numerical solution and ansoln.dat for analytic solution
run: datagenerator.cpp LUdecomp.cpp
	g++ datagenerator.cpp -o run1
	g++ LUdecomp.cpp -lgsl -lgslcblas -o run2
clean:
	rm *.dat run*